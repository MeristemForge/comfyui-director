#!/usr/bin/env node

// Keep the package-level command compatible while the Skill owns the
// self-contained implementation used by Codex and Claude Code.
import "../skills/director-video/scripts/directorctl.mjs";
