# Local Agent CLI prompt optimizer settings are configured in Director Desk.
# Leave the executable path empty to use the selected CLI from PATH.
# Current options include Codex CLI and Claude Code; more local CLIs can be added later.
